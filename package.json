{
  "name": "@makina-corpus/mapbox-gl-path",
  "description": "Create path with or without help of various directions API",
  "scripts": {
    "buildLib": "npm --workspace=@makina-corpus/mapbox-gl-path run build",
    "buildDocs": "npm --workspace=mapbox-gl-path-documentation run build",
    "build": "npm run buildLib && npm run buildDocs",
    "dev": "npm --workspace=@makina-corpus/mapbox-gl-path run start",
    "docs": "npm --workspace=mapbox-gl-path-documentation run dev"
  },
  "repository": "github:makinacorpus/mapbox-gl-path",
  "workspaces": [
    "packages/lib",
    "packages/docs"
  ]
}
