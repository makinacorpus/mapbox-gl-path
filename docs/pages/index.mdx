import Callout from "nextra-theme-docs/callout";
import Map from "../components/Map";
import pathControl from "../examples/introduction";

# Introduction

_Mapbox-gl-path_ allows you to create paths on a map with or without the help of various directions APIs.  
Click the map below to begin drawing a path 👇

<Map pathControl={pathControl} />

### A lot of possibilities 🎉

- You can add new or intermediate point, move, or delete them to create the perfect path.
- You can draw direct or directional paths and change modes whenever you want to meet your needs.
- You have the possibility loop (and unloop) the route.
- Customize all elements on the interface (points, lines, and panels)
- A numerous of methods to interact with this control
- And the possibility to catch all events fired by the user on the map

<Callout emoji="✅">
  It requires Mapbox-gl-js (or Maplibre-gl-js) as a dependency.
</Callout>
